function Modal({text}) {
    return (
        <section className="modal">
            <article>
                <h3>{text}</h3>
            </article>
        </section>
    );
}

export default Modal;